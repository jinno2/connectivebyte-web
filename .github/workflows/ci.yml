name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: [self-hosted, linux, x64, jinno-desktop]
    steps:
      - uses: actions/checkout@v4
      - run: npm test
      - run: node --test test/server.test.js
